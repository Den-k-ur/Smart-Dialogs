// for passing of custom value
const customValue = (val: number) => `${val}px`;

const colors = {
  black: '#000',
  white: '#fff',
  darkBlue: '#4F46E5',
  lightPurple: '#E8E7FF',
  gray: '#E4E4E7',
};

export const Theme = {
  colors,
};
